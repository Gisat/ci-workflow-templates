[project]
name = "example-python-app"
version = "1.0.0"
description = "Example Python application with Docker publishing"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = []

[tool.ruff]
line-length = 120

[tool.pytest.ini_options]
testpaths = ["tests"]
